#Those two lines are the only strictly necessary dependencies for a basic Discord bot using discord.py and dotenv for environment variable management.
discord.py>=2.0.0
python-dotenv>=0.21.0


# Those under this text are not strictly necessary, they can help improve performance on their respective platforms.

# For development and testing
pytest>=7.0.0

# Optional: For better performance with certain features
uvloop>=0.15.0; platform_system != "Windows"

# Optional: For better performance with certain features on Windows
proactor-io-threads>=0.1.0; platform_system == "Windows"

# Optional: For better performance with certain features on macOS
uvloop>=0.15.0; platform_system == "Darwin"

# Optional: For better performance with certain features on Linux
uvloop>=0.15.0; platform_system == "Linux"

# Optional: For better performance with certain features on all platforms
aiohttp>=3.8.0
websockets>=10.0.0
cchardet>=2.1.0
orjson>=3.6.0
aiodns>=3.2.0
PyNaCl>=1.4.0
bcrypt>=3.2.0
psutil>=5.8.0
PyYAML>=5.4.0
lxml>=4.6.0
asyncio>=3.0.0 # Sometimes not Optional but can be included for clarity
httpx>=0.24.0
